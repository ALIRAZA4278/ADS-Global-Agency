# Team portraits

Drop each founder's photo here and point `team.members[].photo` in
`src/lib/site.js` at it, e.g. `photo: "/team/ali.jpg"`.

- Portrait crop, roughly 4:5 (e.g. 1200×1500).
- JPG or WebP, under ~400KB.
- Until a photo is set, the card falls back to an initials panel.
